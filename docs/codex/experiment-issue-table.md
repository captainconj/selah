# Experiment Issue Table

Type: `reference`
State: `clean`

Working audit table. This is not complete, but it is now concrete enough to guide cleanup.

Issue classes:

- `A` = stale interface drift
- `B` = vocabulary confusion or regression
- `C` = English contamination
- `D` = narrative/doc overreach
- `E` = documentation drift

## Table

| Experiment / Area | Current read | Issue classes | Why |
|---|---|---|---|
| `000_the_beginning` | `foundational anchor experiment` | `E` | Cleanest seed in the repo. It tests the classic ELS anchor claim directly and does not need the later fractal mythology to matter. This is a real starting point. |
| `001_gematria_stats` | `broad descriptive baseline` | `D`, `E` | Still exploratory, but mostly a census/statistics pass. The problem is not computation so much as its openness to symbolic overreading once later narrative gets attached. |
| `002_tier1_hypotheses` | `where disciplined exploration starts slipping` | `D`, `E` | Golden sections, pi-derived positions, and “close enough” heuristics mark the transition from measuring the text to fishing symbolic targets. Historically important, but methodologically loose. |
| `003_center_spiral` | `arbitrary geometry probe` | `D`, `E` | Fibonacci spirals from the center may be interesting as play, but there is no clear null model or strong reason this specific geometry should count as evidence. |
| `004_spiral_fold` | `symbolic-width scan` | `D`, `E` | Tests many hand-picked circumferences and asks which produce “interesting patterns.” That is classic selection pressure unless paired with strong controls. |
| `005_chiastic_decomposition` | `serious early structural attempt` | `D`, `E` | One of the better early files. It compares book-level distributions directly. The risk is not the measurement itself but the later leap from weak/high-level similarity to a full palindromic architecture claim. |
| `006_aleph_tav` | `useful lexical census with stale base number` | `D`, `E` | Counting `את`/`ואת` is legitimate. But the script still prints the old `306,269` framing in its reporting, which means even the cleaner early work is text-base contaminated. |
| `007_entropy` | `descriptive information scan` | `E` | Mostly a chapter-level descriptive metric. Low garbage risk by itself, but easy to over-interpret if treated as theological signal rather than chapter statistics. |
| `008_autocorrelation` | `mixed signal plus symbolic lag fishing` | `D`, `E` | Measuring autocorrelation is reasonable. The issue is the curated list of “symbolic lags” and the temptation to narrate whichever lag looks meaningful. |
| `009_mod7_verses` | `counting experiment with numerology pressure` | `D`, `E` | Real verse-level counts, but aimed at mod-7 saturation and framed to reward divisibility findings. Needs controls to be evidence rather than devotional arithmetic. |
| `010_grid_scan` | `selection-pressure scan` | `D`, `E` | Wrap the whole Torah at many symbolic widths, scan for a curated word list, then compare to a few control widths. This can generate garbage fast without a stronger null model and multiple-testing discipline. |
| `011_eleven_letters` | `narrow follow-up on a real count fact` | `E` | The 11-letter Exodus/Numbers difference is a concrete observed fact. This file is a bounded follow-up and is less polluted than the symbolic scan files around it. |
| `012_twin_symmetry` | `substantive but absorbed into later palindrome myth` | `D`, `E` | Chapter-level Exodus/Numbers comparison is one of the more serious early structural tests. Still, this is part of the pathway that later overgeneralized into the dead fractal-palindrome story. |
| `013_monte_carlo` | `important early control, but on the wrong base text` | `D`, `E` | This is one of the healthiest early instincts because it reaches for null modeling. But it bakes in `306,269` and treats the chosen ratio set as the target, so it is not a clean final control. |
| `014_verse_fractal` | `palindrome myth hardening` | `D`, `E` | At this point the framing is openly “push the fractal down one more level.” This is exactly the dead branch later admitted by the repo itself. |
| `015_deep_stream` | `bulk-statistics overreach` | `D`, `E` | Harmonics, sliding-window palindromes, and seven-fold energy curves all feed the bulk-structure story that later controls killed. |
| `016_paint_fractal` | `visualization of a dead claim` | `D`, `E` | Mostly presentation, but it turns the dead fractal-palindrome narrative into persuasive-looking artifact. Archival only unless rebuilt for a claim that still survives. |
| `017_the_fold` | `dead-branch amplifier` | `D`, `E` | This deepens the fold/palindrome thesis and explicitly asks whether the dead ratios reproduce at sub-scales. It belongs to the branch the repo now says was illusion. |
| `018_word_fold` | `resolution study on a dead frame` | `D`, `E` | Technically more careful than some neighbors, but still organized around proving a “fractal” that later controls kill. |
| `019_the_center` | `mixed: center work matters, chiasm sermon does not` | `D`, `E` | The center hunt is historically important and may contain salvageable positional facts. But the file also bundles narrative-chiasm material that sits inside the same dead palindrome ecosystem. |
| `020_center_of_center` | `center-focused, partly salvageable` | `D`, `E` | Leviticus-centered analysis may matter, but it is still framed through internal chiastic/palindrome assumptions that need separation from the actual positional center findings. |
| `021_seven_seals` | `highly rhetorical dead-branch expansion` | `D`, `E` | By here the bulk palindrome story is being preached, not merely tested. Lots of metrics, but all inside the frame the repo itself later says 048 killed. |
| `022_the_weave` | `mixed: thread idea may matter, 441 mythology does not` | `D`, `E` | The every-7th-letter thread construction is a genuine structural operation. But the file is loaded with `441` mysticism and unsupported extrapolation. |
| `025_the_number` | `numerology consolidation` | `D`, `E` | This is where the early number-myth layer coheres: `441`, `333`, `306,269`, Gen 1:1, and modular patterns all get gathered into a single symbolic engine. High contamination risk. |
| `040_wisdom_and_understanding` | `early numerology-heavy model-building` | `D`, `E` | Not broken code, but the experiment mixes raw counts with eager symbolic assignment and treats weak correspondences as architectural evidence. Historical exploration, not clean evidence. |
| `041_self_reference` | `overclaiming early synthesis` | `D`, `E` | The script openly builds a self-referential chain from selected gematria values to structural meaning and includes brittle claims like `441 divides the total gematria exactly`, which no longer survives variant audit as a trustworthy crib. |
| `042_alive` | `rhetorically inflated and partly dead` | `D`, `E` | This is one of the clearest overreach files in the repo. It claims `The Torah is alive` and packages many disputed or now-weakened assertions as settled facts, including divisibility and DNA analogies that should not be treated as evidence. |
| `043_the_genome` | `exploratory comparison, high analogy risk` | `D`, `E` | Useful as a curiosity/control exercise, but the DNA parallel is interpretively dangerous. The methodology depends on arbitrary nucleotide value systems, so results here should never anchor strong claims about the Torah itself. |
| `044_chargaff` | `analogy-heavy and weakly grounded` | `D`, `E` | This is mostly a Torah-vs-DNA analogy machine. It invents pairing schemes and codon analogies that may be interesting as play, but they are not strong evidence and should not be cited as structural proof. |
| `045_the_code` | `biological parallel, not Torah evidence` | `D`, `E` | The file is about the genetic code more than the Torah, then force-connects it back through numbers like `21`, `73`, and `441`. That is selection-heavy synthesis, not a clean experiment on the text. |
| `046_nucleon_arithmetic` | `cross-domain numerology spillover` | `D`, `E` | Whatever may be true about Shcherbak’s arithmetic, this script uses it to smuggle back discredited Torah-number claims such as `441 divides Torah total gematria exactly`. That makes it unsafe as evidence. |
| `048_the_control` / `048-the-fractal-palindrome.md` | `split: control script may matter, published doc is dead` | `D`, `E` | The script is a control-oriented branch, but the doc is not audit-clean. It uses the wrong total letter count (`306,269`) and presents strong recursive-palindrome claims that later control work no longer supports as stated. Treat the doc as archival unless rebuilt from current text and controls. |
| `050_the_number` | `garbage total / bad signature logic` | `D`, `E` | This script opens with the wrong total gematria (`21,120,089`) and then treats divisibility by `7` and `441` as signature-level facts. The whole frame is unstable and should not be trusted without full rebuild. |
| `053_the_seven_divisions` | `mixed: real division fact, contaminated synthesis` | `D`, `E` | The exact ÷7 letter division is real. The surrounding comment block still promotes dead or unstable claims like total-gematria divisibility and machine-level conclusions that now need stronger separation from the measured boundary facts. |
| `054_division_boundaries` | `useful structural check, interpretation needs restraint` | `D`, `E` | This looks like a real boundary-count experiment and is stronger than the surrounding sermon language. The main risk is overreading the modest word-boundary effect into intentional design without stronger statistical controls. |
| `064_silent_axes` | `honest null-ish result with speculative halo` | `D`, `E` | Better than many symbolic experiments because it admits the main finding is weak. Still, the framing leans hard on the machine/axis story, so keep the measured ELS counts separate from the interpretive theology around “silent axes.” |
| `065_breastplate_cipher` / `065-the-machine.md` | `important synthesis with a hard contradiction` | `D`, `E` | This cluster is conceptually important, but the doc still lists total-gematria divisibility as a confirmed crib. That is directly contradicted by [variant-survival-report.md](/docs/experiments/variant-survival-report.md), which says no total-gematria divisibility survives variant comparison. |
| `076_number_catalog` | `selection-pressure machine` | `D`, `E` | This is a catalog of many Torah number sets scanned for axis-factor resonance. It is not fake code, but it is exactly the kind of wide-net pattern mining that can generate impressive-looking nonsense if cited without strict controls. |
| `079_ngram_census` | `foundational raw census, likely solid` | `E` | This is closer to a descriptive inventory than an argument. Good foundation material if later claims cite it carefully and do not smuggle symbolic meaning into the raw frequency tables. |
| `093_level2_thummim` | `intentional restricted mode, but dangerous if cited as general` | `B`, `E` | Explicitly uses `:dict` and says so. Fine as a historical or restricted-mode experiment, not fine if later prose treats it as representative of the full lexicon or oracle-closed vocabulary without saying so. |
| `082_the_reading_machine` | `intentional curated experiment` | `B`, `E` | This is openly built on a curated vocabulary and even includes non-Torah words like `אהבה`, `בינה`, and `שכינה` in its local vocabulary map. That is acceptable only as a bounded prototype, not as evidence for later full-vocabulary claims. |
| `088_stochastic_oracle` | `historical model-building experiment` | `B`, `E` | This runs on curated dictionary words and should be treated as early conceptual work, not as a final statement about the full system. Fine to preserve, bad to overgeneralize. |
| `089_hebbian_oracle` | `historical model-building experiment` | `B`, `E` | It moves toward full Torah forms and looks computationally serious, but it is still part of the earlier exploratory model-building band. Should be cited as exploratory unless later validated by cleaner experiments. |
| `090_higher_order_space` docs | `honest dead-end / negative result` | `E` | One of the healthier documents because it explicitly reports that the bulk tensor structure does not survive shuffle controls. The main issue is file-path/provenance drift in the implementation note (`090_higher_order.clj` vs current tree naming). |
| `055_the_center` | `strong positional experiment` | `E` | Simple, direct, and tied to the coordinate engine. This is closer to a checkable positional claim than a narrative-heavy synthesis. Main issue is minimal provenance/documentation polish, not obvious nonsense. |
| `056_the_turning_sword` | `strong positional experiment` | `E` | Same pattern as 055: specific verse-length and coordinate claim, directly inspectable from the text/space model. Needs standard provenance hygiene, not conceptual downgrade. |
| `057_the_seven_days` | `structural experiment with synthesis creep` | `D`, `E` | The code is direct and mostly inspectable, but the surrounding claims start to layer symbolic interpretation over positional findings. Keep the measured parts distinct from the sermon layer. |
| `058_the_aleph_tav` / `058-aleph-tav-mirrors.md` | `likely substantive, but provenance split` | `D`, `E` | The doc reports quantitative mirror counts, but the runner file in-tree is named differently (`058_the_aleph_tav.clj`) and needs explicit linkage. The measured counts may be solid; the thematic elaboration still needs separation from the raw result. |
| `060_milemarker_mirrors` | `interesting but higher selection-pressure risk` | `D`, `E` | Runnable and specific, but this is the kind of experiment where thematic pairing can outrun the baseline. Needs careful distinction between computed pairings and interpreted significance. |
| `061_four_folds` / `061-the-four-folds.md` | `mixed: strong geometry, expansive synthesis` | `D`, `E` | The fold arithmetic itself is concrete. The larger origami/tabernacle synthesis likely mixes solid counts with looser narrative extension. Good candidate for later line-by-line separation of measured vs inferred. |
| `061b_axis_words` / `061c_compound_folds` | `supporting structural work, not yet directly audited` | `E` | These appear to support 061’s claims. They should be treated as supporting scripts pending closer provenance and output review. |
| `077_preimage_clusters` | `structurally important, doc provenance weak` | `D`, `E` | Preimage is a central reverse-lookup concept and likely real in the code, but the visible doc path is weak and the prose snippets around it are highly interpretive. Needs a cleaner audit pass. |
| `080_word_census` | `foundational and likely solid` | `E` | This looks like a genuine foundation experiment: direct extraction, counts, gematria, preimage indexing. Main issue is that the corresponding polished doc is not obvious in-tree, so provenance from script to published claim should be made clearer. |
| `091_believability_oracle` | `historically important, terminology stale` | `B`, `E` | The code extracts full Torah forms and appears structurally serious, but comments still describe the full vocabulary as `~7,300`, which now needs explicit distinction from the oracle-closed vocabulary. |
| `091b_the_full_quorum` docs | `substantial result, wording and provenance need tightening` | `B`, `D`, `E` | The doc itself uses `12,826` and looks aligned with full-form analysis, but the repo has no separate `091b` experiment file to audit directly. This cluster needs provenance clarity: which code path produced which saved artifacts and under what vocabulary definition. |
| `091b_review` | `useful skepticism, still language cleanup needed` | `B`, `E` | The review is one of the more disciplined docs in the repo, but it still lives inside the same ambiguous vocabulary terminology ecosystem and should inherit the cleaned definitions. |
| `092_grid_permutation` | `strong control experiment, terminology cleanup only` | `B`, `E` | This looks like one of the healthiest experiments in the repo: explicit null model, clear survival/death framing, and the code builds its own oracle context rather than leaning on stale helper glue. Main issue is terminology around vocabulary size and historical comments. |
| `092b_ramban_principle` docs | `strong conceptual control, provenance gap` | `B`, `E` | The doc is disciplined and tied to the permutation story, but there is no visible `dev/experiments/092b_*.clj` file in the current tree. That means the argument may be sound while the code provenance is incomplete in-repo. |
| `094_thummim_sweep` | `mostly solid core result, but terminology needs repair` | `E`, `D` | The sweep itself is over `12,826` forms and has saved data. But surrounding docs still sometimes describe the top vocabulary level as `~7,300 full Torah lexicon`, which blurs lexicon vs oracle-closed vocabulary. |
| `096_basin_classification` | `data-bearing but contaminated` | `C`, `E` | Saved statistics look real and internally consistent, but the experiment code populates `:meaning` via `dict/translate-english` fallback. That means English labels in summaries and downstream docs cannot be trusted without review. |
| `097_per_head_basins` | `data-bearing but contaminated` | `C`, `E` | Same English contamination pattern as 096. Per-head structure may be real, but English labels and any prose built from them need re-checking. |
| `091b_the_full_quorum` | `likely substantial, needs contract check` | `E`, `D` | Docs clearly use `12,826`, which fits Scott's clarification. But this area should be checked for vocabulary-contract wording and whether its prose distinguishes full lexicon from oracle-closed vocabulary. |
| `103_throne_room` | `suspect output script` | `A`, `B` | Uses `o/forward hebrew {:vocab :torah}` and `o/forward-by-head ... {:vocab :torah}` even though current APIs expect a direct selector like `:torah`. |
| `104_the_spies` | `suspect output script` | `A`, `B` | Same stale oracle call shape as above. |
| `106_has_woman_been_bit` | `suspect output script` | `A`, `B` | Same stale oracle call shape and broken basin-path printing. |
| `107_the_bronze_serpent` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `108_ezekiels_chariot` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `109_isaiahs_temple` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `110_revelation_throne` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `111_daniels_thrones` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `112_the_suffering_servant` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `113_the_cross` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `114_psalm_22` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `115_the_cornerstone` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `116_the_binding_of_isaac` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `117_jacobs_ladder` | `suspect output script` | `A`, `B` | Same stale oracle call shape and `(:path walk)` misuse. |
| `118_the_seven_days` | `suspect saved output` | `A`, `B` | Saved output visibly prints `basin path: []`, which is broken plumbing, and the experiment uses stale oracle call shapes. |
| `119_the_fall` | `suspect output script` | `A`, `B` | Same stale oracle call shape and broken basin-path output. |
| `120_the_burning_bush` | `suspect output script` | `A`, `B` | Same stale oracle call shape and broken basin-path output. |
| `121_jacob_wrestles` | `suspect output script` | `A`, `B` | Same stale oracle call shape and broken basin-path output. |
| `122_ruth` | `suspect output script` | `A`, `B` | Same stale oracle call shape and broken basin-path output. |
| `123_the_cure_for_leprosy` | `suspect output script` | `A`, `B` | Same stale oracle call shape and broken basin-path output. |
| `124_the_body_coordinates` | `suspect output script` | `A`, `B` | Same stale oracle call shape and broken basin-path output. |
| `125_the_invisible_blessing` | `suspect saved output` | `A`, `B` | Saved output shows the same empty basin-path symptom and stale oracle call shape. |
| `126_curses_and_blessings` | `suspect output script` | `A`, `B` | Uses stale oracle call shape and directly reads `(:path walk)`. |
| `127_pathogen_prescriptions` | `suspect output script` | `A`, `B` | Uses `(:path walk)` in reported output and should be assumed stale until rerun. |
| `play_leprosy` protein script | `suspect helper` | `A`, `B` | Same stale helper pattern as the passage experiments. |
| `holy_of_holies` script | `mixed` | `B`, `E` | Uses `:dict` in at least one path on purpose. That may be acceptable for a restricted view, but the script needs clear boundary language if cited. |
| `bulk_translate` and `torah-english.edn` | `garbage for evidence, UI-only at best` | `C` | Machine-English output is not suitable for experiments, interpretation, or claims. It should be treated as operator/UI convenience only until curated. |
| `selah.oracle` presentation layer | `needs cleanup` | `B`, `C` | Still describes `:torah` as `~7,300 full` and uses English fallback in `forward` and `rank-phrases`. |
| `selah.basin` presentation layer | `needs cleanup` | `C`, `E` | Basin structure may be valid, but English fallback is baked into reported meanings and comments still use the wrong vocabulary description. |
| `docs/experiments/093-synthesis.md` | `wording repair needed` | `B`, `D`, `E` | Talks about vocabulary invariance but uses the blurred `~7,300 full Torah lexicon` phrasing. Needs sharper technical language. |
| `docs/experiments/093-because-please.md` | `wording repair needed` | `B`, `D`, `E` | Same issue as 093 synthesis. |
| `docs/experiments/091b-the-full-quorum.md` | `strong doc, but not yet audit-clean` | `B`, `D`, `E` | Good use of `12,826`, but should be tied to an auditable experiment path and checked against cleaned terminology for full lexicon vs oracle-closed vocabulary. |
| `docs/experiments/091b-review.md` | `high-signal review, still terminology cleanup` | `B`, `E` | One of the best self-critical docs, but still downstream of the repo’s vocabulary-term drift. |
| `docs/experiments/092-grid-permutation.md` | `high-signal and comparatively healthy` | `B`, `E` | A rare doc that clearly states what survives and what dies. Keep it, but clean the vocabulary terminology and source definitions. |
| `docs/experiments/092b-the-ramban-principle.md` | `high-signal, code provenance incomplete` | `B`, `E` | The writeup is careful and intellectually useful, but it should be anchored to an auditable experiment file or explicitly marked as a writeup without preserved runner code. |
| `docs/experiments/094-thummim-sweep.md` | `strong core doc with wording repairs needed` | `B`, `E` | Uses `12,826` clearly for the sweep itself. Main issue is keeping the sweep result separate from later blurred vocabulary-level language. |
| `docs/experiments/090-higher-order-space.md` | `high-signal negative result` | `E` | Good scientific behavior: it says the tensor/bulk spectral story dies under shuffle controls. Needs cleanup around implementation references and surrounding terminology, not a credibility downgrade. |
| `docs/experiments/048-the-fractal-palindrome.md` | `dead doc / archival only` | `D`, `E` | Wrong base text count (`306,269`) and stronger claims than the current control posture can sustain. If cited as live evidence, this is nonsense. |
| `docs/experiments/065-the-machine.md` | `mixed, but currently not audit-clean` | `D`, `E` | The machine thesis may still matter, but the doc includes at least one hard-invalidated crib: total Torah gematria divisibility as a confirmed invariant. That claim should be burned or rewritten. |
| `docs/experiments/variant-survival-report.md` | `high-signal control doc` | `E` | One of the strongest documents in the repo. It clearly says what survives textual variation and what dies, and it explicitly kills some earlier favored claims. This should be a reference standard for cleanup. |
| `docs/the-journey.md` on `048` | `useful self-correction` | `E` | The repo explicitly says experiment `048` showed the palindrome existed in other long texts and was “letter frequency statistics, not Torah.” That admission is important and should govern cleanup of the early band. |
| `docs/planning/the-big-picture.md` on `048` | `correct principle, uneven enforcement` | `E` | It correctly states that `048` killed the palindrome and says dead experiments should be kept. The problem is that dead palindrome language still lives elsewhere in the repo. |
| Early DNA-parallel band (`037`, `042`-`046`) | `quarantine zone` | `D`, `E` | These files repeatedly lean on “Torah as DNA/genetic code” analogies and reuse numerically flattering claims. Some may contain interesting controls or comparisons, but as a band they are too rhetorically hot to treat as evidence without fresh review. |
| Early total-gematria myth band (`041`, `045`, `046`, `048`, `050`, `051`, parts of `053`, `065`) | `contaminated claim family` | `D`, `E` | Multiple files repeat dead or incompatible statements such as `441 divides the total gematria exactly`, unstable total values, or `306,269`-based framing. This is not one typo; it is a recurring bad narrative layer. |
| Earliest palindrome band (`014`-`021`, parts of `015`, `016`, `017`, `018`) | `dead branch, archival only` | `D`, `E` | The repo’s own retrospective says `048` killed the palindrome story. These files remain historically useful for project archaeology, but not as live evidence. |
| Early structural docs (`055`-`061`) | `generally healthier than late passage scripts` | `D`, `E` | These are closer to inspectable positional/arithmetical claims and seem less infected by stale helper glue. Their main risk is interpretive overreach, not broken interfaces. |
| `docs/the-journey.md` | `mostly aligned, still audit wording` | `D`, `E` | Uses `12,826` correctly in some places, but broad synthesis prose should still be checked against the repaired vocabulary terminology. |

## Cleanup Order

1. Fix `A`: stale experiment helper regressions (`{:vocab :torah}` call shape, `(:path walk)` misuse).
2. Fix `C`: remove machine-English from experiment and evidence paths.
3. Fix `B`: make vocabulary terms precise everywhere.
4. Fix `E`: repair docs, comments, and tool descriptions to match the actual model.
5. Re-evaluate `D`: only after the computation and labels are clean.
