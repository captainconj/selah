# Verified Treasures

*Working ledger of things that have been directly rerun, recomputed, or otherwise verified well enough to treat as real signal.*

Type: `reference`
State: `clean`

## Core Structure

- The Torah core stream is being treated as a `304,850`-letter WLC/Leningrad model.
- The full Torah lexicon is `12,826` forms.
- The oracle-closed vocabulary is smaller, around `7,300`.
- The breastplate structure reproduces as `64` coding positions plus `8` singletons.
- The universal backbone letters on the grid are `נ ו י` with total frequency `26`.
- `את` uses columns `1` and `3`, skipping column `2`.

## Ark Walk

- The Ark column at `a=0, b=8, c=7-12` reproduces directly from live code.
- The foundation floor reproduces with the door word `עשה`.
- The atonement root appears twice on the foundation:
  - `וכפרת`
  - `בכפר`
- The final triplet on the foundation is `זהא`, with `GV=13`.
- On the directory/garden layer, `מביא` is unanimous across all four readers.
- On the directory/garden layer, `את` is unanimous across all four readers.
- The vertical stack `למינהו / והקמתי / מביא` is really there at the reported junction.
- The lamb split reproduces:
  - `כבש`
  - `שכב`
  - same letters
  - same `GV=322`
  - different reader outcomes
- The basin landscape reproduces with strong fixed points including:
  - `אמת`
  - `חיים`
  - `שלום`
  - `ברית`
  - `דם`
  - `אור`
  - `שמים`
  - `מים`
  - `רוח`

## Code / Map Layer

- Experiment `100` now reruns as a proper experiment artifact, not just a notebook fragment.
- Experiment `101` now reruns as a proper experiment artifact, not just a notebook fragment.
- The top row/base mapping by live experiment output comes out `1=A, 2=C, 3=G, 4=U`.
- The raw top correlation is `0.3967800427596649`.
- A second mapping still ties on raw correlation.
- Watson-Crick pairing still breaks that tie in favor of `1=A, 2=C, 3=G, 4=U`.
- The letter→amino-acid assignment score reproduces at `13.888299198575803`.
- The frequency correlation reproduces at `0.6854196827438999`.
- The permutation test is now computed by the namespace itself:
  - `0/10000` exceedances
  - `p = 9.999000099990002E-5`
- `א` remains the unmapped letter.
- `כבש` and `שכב` hit the same stones in the code experiment.
- The translated 72-letter breastplate is now generated directly from the experiment namespace.

## Silent Quarter

- The genome-side silence constraint reproduces from live `dict/torah-words`.
- Total Torah forms: `12,826`
- Forms containing `א`: `2,664`
- Forms containing `ס`: `612`
- Forms containing either: `3,205`
- Impossible fraction under the current map: `25.0%`
- Notable impossible words include:
  - `אדם`
  - `אמת`
  - `ברא`
  - `אלהים`
  - `אהיה`
  - `חסד`
  - `ישראל`

## p53

- Fresh p53 playback reproduces at `393` residues -> `393` Hebrew letters.
- p53 `GV = 25,667`.
- p53 has `179 / 391` readable trigram windows.
- The repeated `David` cluster is real:
  - `דדו` as the top repeated word
  - positions `68, 73, 75, 76, 77, 81, 82, 83, 85`
- `נחש` appears at positions `198` and `261`.
- `שמר` appears at position `331`.
- `קדש` appears at position `354`.
- `יהי` appears at positions `311` and `375`.

## Blood

- Hemoglobin alpha reproduces:
  - `נטה` repeated
  - `זרק` at `139`
  - `עקד` at `88`
- Hemoglobin beta reproduces:
  - `נדע` repeated
  - `עקד` at `61` and `142`
  - `נחש` at `106`

## Serpent

- Serpent toxin reproduces:
  - `מטה` at `10`, `11`, `20`
  - `שקר` at `56`
  - `זרק` at `44`
  - `נפש` at `62`
  - `פדה` at `66`
  - `צור` at `91`

## Genome Voice

- The saved genome-voice headline counts still hold:
  - `שני` at `35,821`
  - `שדי` at `26,958`

## Cleanup Boundaries Crossed

- `096/097` basin artifacts were regenerated from cleaned builders.
- The tracked DNA library artifact set was regenerated from the cleaned DNA path.
- The custom DNA artifact boundary was regenerated through the shared clean path.
- The previously blocked leprosy/TB subset was corrected and rerun with verified accessions.
- Regenerated tracked DNA EDN artifacts no longer carry `:meaning` / `:next-meaning`.
- Regenerated tracked DNA top-word positions are numeric again, not `nil`.

## Custom DNA Boundary

- The custom DNA path now runs through shared `experiment-entry` logic in `selah.dna`.
- The refreshed custom artifact layer now includes:
  - `thc-thcas`
  - `psilocybin-psim`
  - `scopolamine-h6h`
  - `mescaline-omt1`
  - `serotonin-tph2`
  - `human-inmt`
  - `human-insulin`
  - `human-oxytocin`
  - `candida-als3`
  - `lactobacillus-slpa`
  - `tardigrade-dsup`
  - `prion-prp`
  - `malaria-csp`
  - `toxoplasma-sag1`
  - `p53-normal`
  - `p53-r175h`
  - `hbb-normal`
  - `hbb-sickle`
  - `leprae-mmp-i`
  - `leprae-ag85b`
  - `leprae-hsp65`
  - `leprae-sod`
  - `leprae-bacterioferritin`
  - `tb-ag85b`
  - `tb-esat-6`
  - `tb-cfp-10`
  - `tb-hsp65`
  - `tb-katg`
- Corrected disease-target provenance now verifies on disk:
  - `Leprae-MMP-I` = `P46841`
  - `Leprae-Ag85B` = `P31951`
  - `Leprae-Hsp65` = `P09239`
  - `Leprae-SOD` = `P13367`
  - `Leprae-Bacterioferritin` = `P43315`
  - `TB-Hsp65` = `P9WPE7`

## Cautions

- “Verified” here means the thing stands well enough to treat as real signal.
- It does not automatically mean every downstream interpretation is equally verified.
- Some notebook-style experiments still need better artifact boundaries even when their core claims look real.
